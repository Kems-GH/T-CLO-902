apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: back-ingress
spec:
  rules:
    - host: testback.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: back-service
                port:
                  number: 80
